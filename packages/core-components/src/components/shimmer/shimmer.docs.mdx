import { ArgTypes, PRIMARY_STORY, Primary, Meta } from '@storybook/blocks';

import * as ShimmerStories from './shimmer.stories';

<Meta of={ShimmerStories} />

# Shimmer

The shimmer effect is an animated placeholder shown while the actual content is being loaded.

## Attributes

<ArgTypes story={PRIMARY_STORY} />
Changes made to the attributes in the above table will reflect in the example
below:
<Primary />
