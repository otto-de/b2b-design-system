import {
  Story,
  ArgsTable,
  PRIMARY_STORY,
  Primary,
  Source,
  Canvas,
  Meta,
} from '@storybook/blocks';

import * as ParagraphStories from './paragraph.stories';

<Meta of={ParagraphStories} />

# Paragraph Component

This component can be used to render running text. Its a wrapper around the `<p>` tag.

### Example

<Canvas of={ParagraphStories.Default} />

NOTE: All attributes are optional. The component can simply be used like this without specifying any attributes:

<Source code={`<b2b-paragraph>Simple Paragraph</b2b-paragraph>`} />

## Properties

<br />

### Weight

<Canvas of={ParagraphStories.Bold} />

### Size

Can be used for hints and asterisk texts, among others.

<Canvas of={ParagraphStories.Small} />

### Align

#### Center aligned text

<Canvas of={ParagraphStories.CenterAligned} />

#### Right aligned text

<Canvas of={ParagraphStories.RightAligned} />

### Margin

#### No Bottom Margin

<Canvas of={ParagraphStories.NoBottomMargin} />

### Variant

#### Grey

<Canvas of={ParagraphStories.GreyVariant} />

### Display

Per default, the `b2b-paragraph` mimicks the native HTML `display` value, which is `block`.
You can set it to `inline`

## Attributes

<ArgsTable story={PRIMARY_STORY} />
Changes made to the attributes in the above table will reflect in the example below:
<Primary />
