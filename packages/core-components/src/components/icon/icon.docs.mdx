import {
  Story,
  ArgTypes,
  Primary,
  Source,
  Canvas,
  Meta,
} from '@storybook/blocks';

import * as IconStories from './icon.stories';

<Meta of={IconStories} />

# Icon Component (Will be Deprecated instead use Icon-100 or Icon-50)

<Canvas columns={3} withSource="open" withToolbar={false}>
  <Story id="components-assets-icon--story-010-primary" />
</Canvas>

## Properties

### Name

The name of the icon. Each name is prefixed with b2b.

You can find [here](#icon-list) the full list of supported icons.

<br />

### Color

The color of the icon. There are four different variants to ensure
good contrast with different background colors. Per default the icon
will inherit the color of the parent element. When using the icon
in a standalone context and the color of the parent is not defined, we recommend
using one of the variants below.

#### Primary

Use the primary version for light backgrounds.

<Canvas columns={3} withSource="open" withToolbar={false}>
  <Story id="components-assets-icon--story-010-primary" />
</Canvas>

#### Secondary

Use the secondary version for icons that indicate secondary actions.

<Canvas columns={3} withSource="open" withToolbar={false}>
  <Story id="components-assets-icon--story-020-secondary" />
</Canvas>

### Inverse

Use the inverse version for dark backgrounds only.

<Canvas
  style={{ background: '#333333' }}
  columns={3}
  withSource="open"
  withToolbar={false}>
  <Story id="components-assets-icon--story-030-inverse" />
</Canvas>

#### Inherit

Inherit will allow you to specify any color for the icon by first defining the parent `color` property.

<Canvas columns={3} withSource="open" withToolbar={false}>
  <Story id="components-assets-icon--story-035-inherit" />
</Canvas>

<Canvas columns={3} withSource="open" withToolbar={false}>
  <Story id="components-assets-icon--story-050-size-100" />
</Canvas>

### Size (Deprecated)

The icon comes in three different sizes that can be adjusted according
to the context the icon is used in.

#### 50

The smallest icon size at 16 x 16px. Can be used in a small button or label.

#### 100

This is the standard size of the icon, 24 x 24px. If not otherwise specified, this is the default.

#### 200

This is a big icon with a size of 32 x 32px. It should not be used within
a button or menu.

### clickable

Will display a pointer cursor when hovering over the icon.

### Custom Properties

The properties below are CSS variables that we expose in our component. You can set your own value by overwriting the respective variables
in your styles:

```css
--b2b-icon-line-height: 1rem;
```

<table>
  <tr>
    <td>Variable Name</td>
    <td>Description</td>
  </tr>
  <tr>
    <td>--b2b-icon-line-height</td>
    <td>
      The icon line height. Per default it will inherit the current line height
      set on the parent element.
    </td>
  </tr>
</table>

## Attributes

<ArgTypes of={IconStories} />
Changes made to the attributes in the above table will reflect in the example
below:
<Primary />

### Icon List

<Canvas of={IconStories.story070AllIcons} />
