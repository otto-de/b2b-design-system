import { Story, Primary, Source, Canvas, Meta } from '@storybook/blocks';

import * as ScrollableContainerStories from './scrollable-container.stories';

<Meta of={ScrollableContainerStories} />

# Scrollable Container

This component is a helper container that encapsulates the classes to make the content scrollable both horizontally
and vertically. The scroll will only appear when the content is bigger than the container.

This container sets the `white-space` property as `nowrap`, so be cautious to overwrite it in any children if you
require the text wo wrap.

```html
<b2b-scrollable-container style="width: 400px; height: 50px">
  <!-- Any content -->
</b2b-scrollable-container>
```

### Horizontal scroll

In this example the text will inherit the `nowrap` property of the container, therefore is just one long line,
triggering the horizontal scroll on the container.

<Canvas columns={3} withSource="closed" withToolbar={false}>
  <Story id="components-content-scrollable-container--story-010-horizontal-scroll" />
</Canvas>

### Vertical scroll

In this example we set the white-space of the text to normal so that it will wrap, also causing it to be vertically
a long paragraph, triggering the vertical scroll on the container.

<Canvas columns={3} withSource="closed" withToolbar={false}>
  <Story id="components-content-scrollable-container--story-020-vertical-scroll" />
</Canvas>
